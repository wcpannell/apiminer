sudo: false
language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev"
install:
  - "make"
  - if [[ $TRAVIS_PYTHON_VERSION < 3.0 ]]; then pipenv install mock; fi
script: "make test"
jobs:
  include:
    - stage: docs
      python: 3.6
      script: make docs
