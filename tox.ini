[tox]
envlist = py27,py35,py36,py37
# This lets us test before I get setup.py going. Remove after creating setup.py
skipsdist = True

[testenv]
deps = pipenv
commands=
	pipenv install --dev --ignore-pipfile
	pipenv run pytest
